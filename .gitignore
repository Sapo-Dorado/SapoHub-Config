# Per-machine files generated by a nixos-anywhere bootstrap run — never
# committed, regenerated every bootstrap run.
/hardware/generated-hardware-configuration.nix
/hardware/generated-disk-device.nix
